
module.exports = ({ title, author, genre, style }) => {
  return `Book cover for "${title}" by ${author}. Genre: ${genre}. Style: ${style}. High detail, cinematic lighting, dramatic composition.`;
};
